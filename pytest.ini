[pytest]
testpaths = tests
pythonpath = .
# Optional: always emit Allure raw data (then: allure serve reports/allure)
# addopts = --alluredir=reports/allure
markers =
    appium: Android UI tests driven by Appium (run in the appium-android CI job)
