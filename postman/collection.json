{
  "info": {
    "_postman_id": "qa-smoke-placeholder",
    "name": "QA Smoke (API)",
    "description": "Minimal collection for CI. Replace with your real API tests; keep `{{baseUrl}}` or update workflow env vars.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "GET base URL (smoke)",
      "event": [
        {
          "listen": "test",
          "script": {
            "exec": ["pm.test('Status is 200', function () { pm.response.to.have.status(200); });"],
            "type": "text/javascript"
          }
        }
      ],
      "request": {
        "method": "GET",
        "header": [],
        "url": "{{baseUrl}}"
      },
      "response": []
    }
  ],
  "variable": [
    {
      "key": "baseUrl",
      "value": "https://example.com"
    }
  ]
}
